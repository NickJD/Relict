"""Top-level Relict package shim for backward-compatible imports.

This file makes the project root importable as package `Relict` so the
console entrypoint `relict=Relict.cli:main` resolves.
"""

__version__ = "0.1"

